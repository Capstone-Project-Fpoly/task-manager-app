import 'package:flutter/material.dart';

class BroadPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Broad Page'),
    );
  }
}